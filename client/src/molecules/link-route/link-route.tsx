import { FC } from 'react';
import LinkRouteProps from './dto';

export const LinkRoute: FC<LinkRouteProps> = (
  props: LinkRouteProps
): JSX.Element => {
  return <div></div>;
};
export default LinkRoute;
